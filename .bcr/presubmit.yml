bcr_test_module:
  module_path: .
  matrix:
    bazel: [8.x, 9.x, rolling]
    platform: [debian11, ubuntu2004]
  tasks:
    run_test_module:
      name: Test linux.bzl
      bazel: ${{ bazel }}
      platform: ${{ platform }}
      test_targets:
        - //...
